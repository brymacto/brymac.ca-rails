<article class="project projectDetail">
  <h3 class="projectDetail-header project-header">
    <%= link_to @project.title, @project  %></h3>
    <div class="projectDetail-meta project-meta">
      <span class="projectDetail-date project-date"><%= @project.public_date %></span>
      <div class="tag_list">
        <% @project.tag_list.each do |tag| %>
        <%= link_to tag, ('/tags/' + tag.downcase), class: :badge  %>
        <% end %>
      </div>
    </div> 
      <% if @project.photo.url %>
        <%= image_tag(@project.photo.full, class: 'full') %>
        <div class="caption"><%= @project.photo_caption %></div>
      <% end %>
    <p class="projectDetail-contents">
      <%= @project.description.html_safe %> 
    </p>
  </article>